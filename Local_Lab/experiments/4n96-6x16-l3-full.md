# 4n96 6x16 L3-balanced cumulative full validation

- Repository anchor: `1145b472d751d741bddb7afbb50366669bff4430`.
- Model-source anchor: `e7e0ce1`.
- No-profile binary SHA-256:
  `fe0049c067b8a0efec3385c49dd9e606001d91444f7fcf176990a9f8f99f9c1e`.
- Configuration: 4 nodes, 96 ranks, 24 ranks/node, `6x16`, complete
  2592/12960 steps, L3-balanced NUMA-row rankfile.

## Warrant

The validated default-binding `6x16` full result is `2461.22 s`.  Same-
allocation ABBA ratios for NUMA-row placement and its L3-balanced refinement
were `105.620/108.765` and `102.570/103.845`; their product projects about
`2361 s`.  The remaining difference to the `2350 s` target is only about
11 seconds and short score/full scaling is not exact, so a real no-profile
complete run is now more informative than another extrapolation.

The launcher performs a same-allocation 60/300 no-profile preflight and aborts
nodes slower than 90 seconds, proves the 96-rank core mapping, preserves full
output cadence, finalizes both comparisons, and runs an unmodified copy of the
official validator except for `dir_test`.

## Result

- Slurm job: `118850664`, nodes `j05r2n[14-17]`.
- Preflight: `68.20 s`, normal end and comparison PASS.
- Complete three-day wall: `2425.99 s`.
- `run_report.json`: `passed=true`, `normal_end=true`, outputs and comparison
  PASS; all 26 variables have `RMSE=0` and `max_abs=0`.
- Official `vali.py`: PASS with the required final verdict.

This is `35.23 s` (`1.43%`) faster than the validated default-binding `6x16`
result (`2461.22 s`) and `73.75 s` (`2.95%`) faster than the earlier `8x12`
result (`2499.74 s`).  It is the new fastest validated complete run, but it is
still `75.99 s` above the `2350 s` target, so it does not by itself complete
the optimization goal.

Artifacts:

- Preflight: `Local_Lab/runs/profile128/final-6x16-l3-preflight-20260809T211904Z_20260809T211907Z_12131`
- Full: `Local_Lab/runs/profile128/final-6x16-l3-full-noprofile-20260809T211904Z_20260809T211908Z_13067`
